datasets:
  - bcnb

task_col: pr
extra_cols: []  # Extra columns to copy from raw metadata to split TSVs
task_type: classification
metrics:
  - macro-ovr-auc
label_dict:
  0: negative
  1: positive

sample_col: case_id
num_samples: 1058
